(function(){
const $=id=>document.getElementById(id),E=v=>typeof esc==='function'?esc(v):String(v??'');const KEY='simple_store_supplier_orders_v1';
function get(){try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch(e){return[]}}function put(a){localStorage.setItem(KEY,JSON.stringify(a))}function status(s){return({draft:'טיוטה',ordered:'הוזמן',partial:'התקבל חלקית',received:'התקבל במלואו'})[s]||s}function now(){return new Date().toISOString()}
function shell(){let admin=$('admin');if(!admin)return null,menu=admin.querySelector('.adminmenu');if(menu&&!$('supplierOrdersMenuBtn')){let b=document.createElement('button');b.id='supplierOrdersMenuBtn';b.className='green';b.textContent='הזמנות ספקים';b.onclick=()=>{adminTab('supplierOrdersTab');renderSupplierOrders()};let r=$('reorderMenuBtn');r?.insertAdjacentElement('afterend',b)||menu.prepend(b)}let tab=$('supplierOrdersTab');if(!tab){tab=document.createElement('div');tab.id='supplierOrdersTab';tab.className='adminTab hidden';admin.appendChild(tab)}return tab}
function edit(id){let a=get(),o=a.find(x=>x.id===id);if(!o)return;let tab=shell(),ps=typeof activeProducts==='function'?activeProducts():(window.prods||[]);tab.innerHTML=`<div class="card"><div class="bar"><h2>${o.supplier?'הזמנה — '+E(o.supplier):'הזמנת ספק חדשה'}</h2><button onclick="renderSupplierOrders()">חזרה</button></div><label>שם הספק<input id="soSupplier" value="${E(o.supplier||'')}"></label><label>הערה<input id="soNote" value="${E(o.note||'')}"></label><div id="soLines"></div><button id="soAdd" class="light">הוסף מוצר</button><div style="margin-top:12px;display:flex;gap:8px;flex-wrap:wrap"><button id="soSave" class="blue">שמור טיוטה</button><button id="soOrdered" class="green">סמן כהוזמן</button>${o.status==='ordered'||o.status==='partial'?'<button id="soReceive" class="orange">קבלת סחורה</button>':''}</div></div>`;function lines(){let box=$('soLines');box.innerHTML=o.items.map((x,i)=>`<div style="display:grid;grid-template-columns:2fr 1fr 1fr auto;gap:7px;margin:7px 0"><select data-i="${i}" class="soP">${ps.map(p=>`<option value="${p.id}" ${String(p.id)===String(x.product_id)?'selected':''}>${E(p.name)}</option>`).join('')}</select><input data-i="${i}" class="soQ" type="number" min="1" value="${Number(x.qty||1)}"><input data-i="${i}" class="soC" type="number" min="0" step="0.01" placeholder="מחיר קניה" value="${x.cost??''}"><button data-i="${i}" class="soX">×</button></div>`).join('');box.querySelectorAll('.soX').forEach(b=>b.onclick=()=>{o.items.splice(Number(b.dataset.i),1);lines()})}lines();$('soAdd').onclick=()=>{let p=ps[0];if(p)o.items.push({product_id:p.id,name:p.name,qty:1,cost:''});lines()};function collect(){o.supplier=$('soSupplier').value.trim();o.note=$('soNote').value.trim();o.items=[...document.querySelectorAll('.soP')].map((el,i)=>{let p=ps.find(p=>String(p.id)===String(el.value));return{product_id:el.value,name:p?.name||'',qty:Math.max(1,Number(document.querySelectorAll('.soQ')[i].value)||1),cost:Number(document.querySelectorAll('.soC')[i].value)||0,received:Number(o.items[i]?.received||0)}});o.updated_at=now()}$('soSave').onclick=()=>{collect();put(a);renderSupplierOrders()};$('soOrdered').onclick=()=>{collect();o.status='ordered';o.ordered_at=now();put(a);renderSupplierOrders()};if($('soReceive'))$('soReceive').onclick=()=>receive(id)}
async function receive(id){let a=get(),o=a.find(x=>x.id===id);if(!o)return,ps=typeof activeProducts==='function'?activeProducts():(window.prods||[]),tab=shell();tab.innerHTML=`<div class="card"><div class="bar"><h2>קבלת סחורה — ${E(o.supplier||'')}</h2><button onclick="renderSupplierOrders()">חזרה</button></div><div class="msg">הזן כמה התקבל עכשיו. המלאי יתעדכן רק לאחר אישור.</div>${o.items.map((x,i)=>`<div style="display:grid;grid-template-columns:2fr 1fr 1fr;gap:8px;align-items:center;margin:9px 0"><b>${E(x.name)}</b><span>הוזמן ${x.qty} · התקבל ${x.received||0}</span><input class="soRec" data-i="${i}" type="number" min="0" max="${Math.max(0,x.qty-(x.received||0))}" value="${Math.max(0,x.qty-(x.received||0))}"></div>`).join('')}<button id="soConfirmRec" class="green">אשר קבלה ועדכן מלאי</button></div>`;$('soConfirmRec').onclick=async()=>{let ins=[...document.querySelectorAll('.soRec')];try{for(let el of ins){let i=Number(el.dataset.i),x=o.items[i],qty=Math.max(0,Number(el.value)||0);if(!qty)continue;let p=ps.find(p=>String(p.id)===String(x.product_id));if(!p)continue;let newStock=Number(p.stock_quantity||0)+qty,patch={stock_quantity:newStock};if(Number(x.cost)>0)patch.purchase_price=Number(x.cost);await req('/rest/v1/products?id=eq.'+encodeURIComponent(p.id),{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify(patch)});try{await req('/rest/v1/inventory_movements',{method:'POST',body:JSON.stringify({product_id:p.id,change_qty:qty,reason:'קבלת הזמנת ספק',note:'ספק: '+(o.supplier||'')})})}catch(e){}Object.assign(p,patch);x.received=Number(x.received||0)+qty}o.status=o.items.every(x=>Number(x.received||0)>=Number(x.qty||0))?'received':'partial';o.updated_at=now();put(a);await window.load?.();renderSupplierOrders()}catch(e){alert('קבלת הסחורה נכשלה: '+(e.message||e))}}}
window.renderSupplierOrders=function(){let tab=shell();if(!tab)return,a=get();tab.innerHTML=`<style>.so-order{border:1px solid #e1e7ef;border-radius:12px;padding:12px;margin:9px 0}</style><div class="card"><div class="bar"><div><h2 style="margin:0">הזמנות ספקים</h2><div class="msg">טיוטה → הוזמן → התקבל חלקית → התקבל במלואו</div></div><button id="soNew" class="green">הזמנה חדשה</button></div></div><div class="card">${a.length?a.sort((x,y)=>String(y.updated_at).localeCompare(String(x.updated_at))).map(o=>`<div class="so-order"><div class="bar"><div><b>${E(o.supplier||'ללא שם ספק')}</b><div class="msg">${status(o.status)} · ${o.items.length} מוצרים · ${new Date(o.updated_at).toLocaleString('he-IL')}</div></div><button onclick="editSupplierOrder('${o.id}')">פתח</button></div></div>`).join(''):'<div class="msg">עדיין אין הזמנות ספקים.</div>'}</div>`;$('soNew').onclick=()=>{let a=get(),o={id:'so_'+Date.now(),supplier:'',note:'',status:'draft',items:[],created_at:now(),updated_at:now()};a.push(o);put(a);edit(o.id)}};window.editSupplierOrder=edit;window.ensureSupplierOrders=shell;setTimeout(shell,1300);
})();